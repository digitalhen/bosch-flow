# Secrets & runtime state — never commit these
bosch_tokens.json
bosch_users.json
bosch_webhooks.json
bosch_poller_state.json
bosch_events.jsonl

# Python
.venv/
__pycache__/
*.pyc

# macOS menu bar app build artifacts
menubar/build/
menubar/Bosch Bar.app/
# generated per release (published to GitHub Releases, not committed)
menubar/dist/appcast.xml
menubar/dist/_notarize.zip
menubar/dist/*.zip

# macOS
.DS_Store

# PyInstaller (only if run from repo root; build.sh keeps output under menubar/build)
/build/
/dist_pyi/
/build_pyi/
*.spec
